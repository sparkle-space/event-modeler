services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      SECRET_KEY_BASE: ${SECRET_KEY_BASE:-please-change-me-this-is-a-placeholder-key-base-that-is-at-least-64-bytes-long}
      PHX_HOST: ${PHX_HOST:-localhost}
      AUTH_PASSWORD: ${AUTH_PASSWORD:-}
